{
  "column_default": "markers",
  "logging_config": {
    "version": 1,
    "disable_existing_loggers": false,
    "formatters": {
      "f": {
        "format": "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
      }
    },
    "handlers": {
      "default": {
        "class": "logging.StreamHandler",
        "formatter": "f",
        "level": "INFO"
      }
    },
    "loggers": {
      "": {
        "handlers": [
          "default"
        ],
        "level": "INFO",
        "propagate": false
      }
    }
  }
}
